import { config } from 'dotenv';
config({ path: '.env.local' });

import { drizzle } from 'drizzle-orm/neon-http';
import { neon } from '@neondatabase/serverless';
import { posts } from './schema';

const sql = neon(process.env.DATABASE_URL!);
const db = drizzle({ client: sql });

const post = {
  title: 'Oracle Buffer Cache: Architecture, LRU Mechanics, and Tuning',
  slug: 'oracle-buffer-cache-architecture-lru-tuning',
  excerpt:
    'A comprehensive guide to Oracle buffer cache internals — covering the modified LRU touch-count algorithm, DB_CACHE_SIZE sizing methodology using V$DB_CACHE_ADVICE, the three buffer pools (KEEP/RECYCLE/DEFAULT) and when to use each, and diagnosing hot block contention and cache pressure using V$BH, buffer busy waits, and physical I/O wait event analysis.',
  category: 'oracle-database' as const,
  published: true,
  isPremium: false,
  publishedAt: new Date('2026-06-05'),
  youtubeUrl: null,
  content: `## Introduction

The Oracle buffer cache is the largest and most performance-critical component of the System Global Area (SGA). Its purpose is straightforward: cache database blocks in memory so that subsequent reads find the block already in memory — a logical read, also called a cache hit — rather than fetching it from disk, which is a physical read and orders of magnitude slower. Every SQL statement that touches data must obtain those data blocks in the buffer cache before it can read or modify them. This makes the buffer cache the intersection point of virtually every database operation, and its behaviour — how aggressively it retains useful blocks, how efficiently it recycles cold buffers, and how it handles contention under high concurrency — directly determines the throughput and response time of the database.

The ratio of logical reads to physical reads — the buffer cache hit ratio — was historically the primary Oracle performance metric. An installation guide from the early Oracle 8 era would instruct the DBA to keep the hit ratio above 95% and increase the buffer cache if it dropped below that threshold. Oracle later moved decisively away from hit ratio as the single tuning target, because a 99% hit ratio can still be hiding severe performance problems if the 1% of cache misses are on the critical path of a high-frequency transaction executing thousands of times per second. The hit ratio aggregates across all SQL and all blocks; it cannot tell you that the 1% of misses is costing 40% of total response time for your most important transaction.

The modern approach is more surgical: size the buffer cache generously so that the OLTP working set fits comfortably, then use AWR wait event data (\`db file sequential read\`, \`db file scattered read\`) and \`V$BH\` hot block analysis to identify specific contention. The question is not "is the cache big enough on average?" but rather "are the specific blocks that matter being retained, and are they being contended for?" Average miss rates are irrelevant; miss rates on index root blocks, sequence cache blocks, and core lookup tables are critical. This shift in diagnostic philosophy — from averaged statistics to targeted wait event analysis — is what separates effective buffer cache tuning from cargo-cult DBA work.

The introduction of AMM (Automatic Memory Management) and ASMM (Automatic Shared Memory Management) in Oracle 10g/11g shifted much of the initial sizing work to Oracle's memory advisors, but understanding the underlying mechanics remains essential for diagnosing cache pressure, buffer busy waits, and hot block contention. When the Memory Advisor cannot help — because it optimises for average physical read rates, not for hot block contention or for the cache effects of a new batch workload added to an existing OLTP system — the DBA must understand the LRU algorithm, the buffer pool architecture, and the interplay between foreground processes and DBWR well enough to intervene effectively.

---

## SGA Architecture: Where the Buffer Cache Lives

The SGA is the shared memory region that Oracle allocates at instance startup. Its components are allocated from a contiguous block of memory (or from distributed granules in more recent Oracle versions), and each component serves a distinct purpose. The buffer cache typically consumes 50–80% of the SGA — it is by far the largest component in most OLTP configurations. The shared pool (containing the library cache for parsed cursor storage and the data dictionary cache for row cache entries) is the second-largest component, typically 15–30% of the SGA. The large pool is used for parallel query slave buffers, RMAN backup/restore work areas, and shared server dispatcher buffers. The Java pool and streams pool serve their respective features. The redo log buffer is a circular buffer that holds redo entries generated by foreground processes before LGWR writes them to the online redo log files. The fixed SGA is a small reserved region containing Oracle's own internal data structures and is not adjustable.

With ASMM (\`SGA_TARGET\` set to a non-zero value and individual pool sizes left at 0 or set as minimum floors), Oracle's Memory Advisor dynamically resizes pools based on observed demand — the buffer cache shrinks when the shared pool needs more library cache memory to accommodate a parse storm and grows back when SQL activity stabilises. The algorithm uses statistics from \`V$DB_CACHE_ADVICE\` and \`V$SHARED_POOL_ADVICE\` to estimate the benefit of transferring granules between components. This works well in practice for systems with steady-state workloads. It performs less well when workload mix changes sharply — for example, when a large batch job starts running at night and the pattern of SQL, block access, and memory pressure is entirely different from the OLTP day pattern. In those cases, setting explicit minimum floors for the buffer cache (\`DB_CACHE_SIZE\`) and shared pool (\`SHARED_POOL_SIZE\`) ensures Oracle cannot shrink either component below the level needed for its baseline workload.

With AMM (\`MEMORY_TARGET\` set), Oracle manages both SGA and PGA together. PGA memory (sort areas, hash join work areas, bitmap merge areas) is drawn from the same pool as SGA components. This sounds attractive but is operationally problematic on large systems: when a PGA-intensive operation (a parallel hash join, for example) causes Oracle to shrink the buffer cache to donate memory to PGA, the resulting wave of physical reads can cascade into a performance crisis. Most production Oracle shops running large OLTP workloads disable AMM and manage SGA and PGA separately with \`SGA_TARGET\` and \`PGA_AGGREGATE_TARGET\` instead.

The \`DB_CACHE_SIZE\` parameter sets the DEFAULT buffer pool size. When \`SGA_TARGET > 0\`, setting \`DB_CACHE_SIZE\` to a non-zero value establishes a minimum floor — Oracle will grow the buffer cache above this floor but never shrink it below it. When \`SGA_TARGET = 0\`, \`DB_CACHE_SIZE\` is the static allocation for the DEFAULT pool. \`DB_KEEP_CACHE_SIZE\` and \`DB_RECYCLE_CACHE_SIZE\` set the KEEP and RECYCLE pool sizes; these are always static allocations — ASMM does not manage them. The granule is the fundamental unit of SGA allocation, typically 4 MB for SGAs under 1 GB and 16 MB for larger SGAs. All SGA resizing operations happen in granule increments; you cannot grow the buffer cache by 3 MB — the smallest increment is one granule. This matters when sizing: requesting a buffer cache that is not a multiple of the granule size causes Oracle to round up to the next granule boundary.

---

## The LRU Algorithm: Touch Count and Buffer Chains

The buffer cache uses a modified LRU (Least Recently Used) algorithm based on touch counts rather than strict access recency. Pure LRU — where the buffer accessed most recently is always kept and the least recently accessed is always evicted — has a well-known failure mode called the "scan pollution" problem: a full table scan that reads every block in a large table exactly once displaces the entire buffer cache with blocks that will never be accessed again, replacing the working set of frequently used OLTP blocks. Oracle's touch count variant avoids this by distinguishing between blocks that are genuinely hot (frequently accessed) and blocks that merely passed through the cache once.

When a buffer is first read into the cache it is placed at the midpoint of the LRU list — this is called a "cold" buffer. It enters with a touch count of 1. Each time the buffer is subsequently accessed its touch count increments. Buffers with a touch count above a threshold (controlled by the hidden parameter \`_db_aging_hot_criteria\`, defaulting to 2) are promoted to the "hot" end of the LRU list and are protected from replacement. Buffers at the cold end with low touch counts (typically 0 or 1) are the first candidates for replacement when Oracle needs a free buffer for a new block. This means a block that is accessed three times in a short window moves to the hot end and stays there, while a block from a full table scan that is read once and never again stays cold and is quickly evicted. The midpoint insertion policy is fundamental to how Oracle avoids scan pollution; understanding it explains why RECYCLE pool assignment is important for large infrequently scanned tables — without it, those blocks enter the DEFAULT pool's LRU list and compete with OLTP hot blocks.

The CBC (Cache Buffer Chains) latch protects the hash chains that link all buffers for a given block address. When a session needs to access a buffer, it must first acquire the CBC latch for the hash bucket containing that block's address, then walk the chain to find the buffer. This is a very short-held, very frequently acquired latch in a busy system — Oracle has highly optimised this code path. However, when a single block is extremely hot — accessed by thousands of sessions per second simultaneously — the CBC latch for that block's hash bucket becomes a serialisation point. Multiple sessions simultaneously trying to acquire the CBC latch for the same bucket causes \`latch: cache buffers chains\` wait events. This is the definitive signature of a hot block problem, and it cannot be solved by adding more memory; it requires reducing contention for the specific block (sequence cache, index root block, or segment header) through application or schema-level changes.

The write list tracks dirty buffers — blocks that have been modified in the cache but not yet written to their datafiles on disk. DBWR (Database Writer) is the background process responsible for writing dirty buffers to disk. It is triggered by several mechanisms: a checkpoint (triggered by the log writer crossing the checkpoint position or by a periodic checkpoint), when the dirty list length reaches a threshold percentage of the buffer cache (controlled by \`_db_writer_max_dirty_pct\`), or when a foreground process needs a free buffer and cannot find one after scanning the LRU tail — the "free buffer waits" scenario. The last case is the most operationally significant: if DBWR cannot keep up with the write workload and dirty buffers fill the cache, foreground processes stall on \`free buffer waits\`, which appears in \`V$SYSTEM_EVENT\` and indicates that DBWR is a bottleneck. The solution is either to increase I/O throughput (more DBWR processes, faster storage), add more buffer cache capacity, or reduce the DML rate.

---

## Buffer Pools: DEFAULT, KEEP, and RECYCLE

Oracle provides three distinct buffer pools within the buffer cache. The DEFAULT pool is the general-purpose pool — every database object resides in the DEFAULT pool unless explicitly assigned to another pool, and ASMM manages its size dynamically. The KEEP pool and RECYCLE pool are static, dedicated allocations that exist alongside the DEFAULT pool and serve opposite purposes.

The KEEP pool is designed for objects that should remain in cache permanently — small reference tables, code lookup tables, frequently accessed index root and branch blocks, and sequence segment blocks. Objects assigned to the KEEP pool (\`STORAGE (BUFFER_POOL KEEP)\`) are never aged out to make room for other objects in the KEEP pool. Once a KEEP-pool object has been loaded into memory (typically by running a full scan or warming it up explicitly), it remains there for the life of the instance. The KEEP pool is sized with \`DB_KEEP_CACHE_SIZE\`, and the total size of all KEEP-pool objects must fit within this allocation — if KEEP-pool objects collectively exceed \`DB_KEEP_CACHE_SIZE\`, buffers will be evicted from the KEEP pool by the normal LRU mechanism. The assignment syntax is \`ALTER TABLE t STORAGE (BUFFER_POOL KEEP)\` for tables, and \`ALTER INDEX i STORAGE (BUFFER_POOL KEEP)\` for indexes. The KEEP pool assignment is stored persistently in the data dictionary and applies immediately without requiring object rebuild.

The RECYCLE pool is the inverse of KEEP: it is designed for large objects that are accessed infrequently and should not be allowed to pollute the DEFAULT pool. A 200 GB historical data table that is scanned once per night by a batch reporting job is the canonical RECYCLE pool candidate. Without a RECYCLE pool assignment, that nightly scan reads millions of blocks through the DEFAULT pool, aging out the OLTP working set and causing a mass cache cold start for all OLTP transactions that execute in the hours following the batch job. The cold-start effect can persist for 30–60 minutes depending on the DEFAULT pool size and the OLTP working set size. By assigning the large scan table to the RECYCLE pool (\`ALTER TABLE t STORAGE (BUFFER_POOL RECYCLE)\`), the scan's blocks are read into and immediately evicted from a dedicated small pool, and the DEFAULT pool is completely untouched. The RECYCLE pool does not need to be large — it only needs to hold the maximum number of blocks that a single pass of the scan requires simultaneously, which is typically a few hundred megabytes even for very large tables.

Multiple block size buffer caches add another dimension to buffer pool architecture. Standard Oracle installations use a single block size (\`DB_BLOCK_SIZE\`, typically 8 KB) for all tablespaces. However, tablespaces can be created with non-standard block sizes (2 KB, 4 KB, 16 KB, 32 KB), and each non-standard block size requires its own dedicated buffer cache, configured with \`DB_2K_CACHE_SIZE\`, \`DB_4K_CACHE_SIZE\`, \`DB_16K_CACHE_SIZE\`, or \`DB_32K_CACHE_SIZE\`. These separate caches are not managed by ASMM and must be sized statically. Non-standard block sizes are used in specialised scenarios: very small block sizes (2 KB or 4 KB) for tablespaces containing many small rows where standard 8 KB blocks would have high internal fragmentation, or very large block sizes (16 KB or 32 KB) for tablespaces that are exclusively scanned sequentially and benefit from larger I/O operations.

---

## Cache Hit Ratio vs. Physical Read Wait Time

The buffer cache hit ratio formula is: \`1 - (physical_reads / (db_block_gets + consistent_gets))\`. In this formula, \`db_block_gets\` counts current mode reads (typically DML operations that need the current version of a block), \`consistent_gets\` counts consistent read accesses (queries that may use CR blocks), and \`physical_reads\` counts the subset of those accesses where the block was not found in the buffer cache and had to be read from disk. A ratio above 95% is generally considered healthy for OLTP workloads; below 90% suggests the cache is either too small for the working set or there is excessive full table scan activity loading blocks that are used only once.

But the hit ratio is a lagging, averaged metric — it tells you that cache misses happened but cannot tell you which blocks, which SQL statements, or what the latency impact was. A database with a 98% hit ratio might still have terrible OLTP response times if that 2% of misses is clustered on a handful of critical index blocks that are accessed thousands of times per second and consistently missed due to cache pressure from a concurrent reporting workload. Conversely, a system with a 93% hit ratio might perform perfectly well if the 7% of misses are against a large RECYCLE-pool table scanned infrequently and the misses have no impact on OLTP latency.

The more actionable metrics are the physical I/O wait times. \`db file sequential read\` is the wait event for single-block reads — typically index range scans and table access by rowid. Its average wait time should be below 1 ms on NVMe or all-flash storage and below 5 ms on a well-configured SAN. \`db file scattered read\` is the wait event for multiblock reads — full table scans and fast full index scans. Its average should be below 2 ms on flash and below 10 ms on spinning disk. A system with a 98% hit ratio but an average \`db file sequential read\` of 15 ms is performing poorly because each cache miss is expensive — the combination of miss frequency and miss cost determines performance, not miss rate alone. If the misses are on critical OLTP index lookups, even a very low miss rate at high latency will be visible in user-facing response times.

The complete diagnostic picture requires combining three data points: the buffer cache hit ratio trend (from \`DBA_HIST_BUFFER_POOL_STAT\`), the average physical I/O wait times (from \`V$SYSTEM_EVENT\` and the corresponding AWR view \`DBA_HIST_SYSTEM_EVENT\`), and the top SQL statements by physical reads per execution (from \`V$SQL\`). The combination reveals whether the problem is global cache undersizing (hit ratio falling, many SQL statements affected), scan pollution (hit ratio degrading only after a batch window, recovering slowly as cache warms), or a specific SQL problem (one or two statements with anomalously high disk reads per execution that can be tuned with indexes or result cache).

---

## Hot Blocks and Buffer Busy Waits

A \`buffer busy waits\` event occurs when a session needs to read or modify a buffer that another session is currently reading from disk or currently modifying. The first session holds a pin on the buffer header — a lightweight exclusive lock that prevents concurrent access while the I/O or modification is in progress — and the second session must wait for the pin to be released. In a database under normal load, individual \`buffer busy waits\` events are extremely short-lived (microseconds to low milliseconds) and represent a normal concurrency control mechanism. They become a performance problem when the same small set of blocks — hot blocks — is being pinned repeatedly at very high frequency, causing a long queue of waiting sessions.

Hot block scenarios are well-categorised in the Oracle community because they arise from predictable architectural patterns. Sequence cache blocks are among the most common: every INSERT that obtains its primary key value from a sequence accesses the sequence's segment header block to read the current cached value and update it. If the sequence cache is too small (the default \`CACHE 20\` in many environments) and the INSERT rate is high (thousands per second across many concurrent sessions), the sequence block becomes extremely hot. The fix is straightforward: increase the sequence cache to 1,000 or 10,000 with \`ALTER SEQUENCE seq_name CACHE 10000\`. The high-water mark (HWM) block of a segment being concurrently extended by multiple sessions is another classic hot block: when multiple sessions simultaneously run the first INSERT into a new extent, all of them access the segment header to update the HWM. Automatic Segment Space Management (ASSM) alleviates this by using bitmap free space tracking rather than freelist contention. Index root blocks are a third pattern: the root block of a B-tree index is accessed by every index lookup for that index. A heavily used unique index on a high-frequency lookup table — accessed millions of times per hour — has an extremely hot root block. Reverse key indexes distribute the hot block problem across multiple leaf blocks (at the cost of disabling range scans); hash partitioning distributes access across multiple partition indexes.

Identifying the hot block from a running system uses the \`P1\`, \`P2\`, and \`P3\` columns of the \`buffer busy waits\` and \`read by other session\` events in \`V$SESSION_WAIT\` or \`V$SESSION\`. \`P1\` is the absolute file number, \`P2\` is the block number within the file, and \`P3\` is the reason code (1 = waiting for a buffer being read from disk; 200 = waiting for a buffer being modified). With \`P1\` and \`P2\` in hand, \`V$BH\` can be joined to \`DBA_EXTENTS\` and \`DBA_OBJECTS\` to identify the owner, object name, object type, and subobject (partition name) that the hot block belongs to. This identification is the pivot point of the investigation: once you know the object and understand whether it is a sequence segment, an index root block, a table segment header, or a specific frequently accessed data block, the appropriate mitigation becomes clear.

---

## Direct Path Reads and Cache Bypass

Oracle 11g introduced intelligent direct path reads for large table full scans — a feature that fundamentally changed the relationship between full table scans and the buffer cache. When Oracle determines that a table is large enough (above a threshold based on the undocumented parameter \`_small_table_threshold\`, which defaults to approximately \`2 × _db_file_multiblock_read_count × CPU_count\` blocks, roughly 2,500 to 10,000 blocks depending on configuration), it reads that table via direct path I/O. Direct path reads bypass the buffer cache entirely: the blocks are read directly from the datafiles into the session's PGA work area (sort or hash area) without ever being placed in the buffer cache. This prevents large scans from polluting the buffer cache with single-use blocks.

Direct path reads generate \`direct path read\` wait events rather than \`db file scattered read\` wait events — this is the diagnostic signature that distinguishes a cache-bypassing scan from a cache-filling scan. The distinction matters when diagnosing wait event profiles: a surge in \`direct path read\` events indicates large table scans happening via direct path and has no impact on the buffer cache. A surge in \`db file scattered read\` events may indicate either that the scanned table is below the direct path threshold (and is filling the cache) or that direct path has been explicitly disabled. Serial direct path reads can be forced for all tables regardless of size with \`ALTER SESSION SET "_serial_direct_read" = ALWAYS\`, or suppressed with \`ALTER SESSION SET "_serial_direct_read" = NEVER\` — the latter is sometimes used when it is known that a moderate-sized table benefits from caching even on a first full scan, but this is rarely the correct intervention and should be tested carefully.

Parallel query always uses direct path reads, regardless of table size or any \`_serial_direct_read\` setting. A parallel full table scan of even a very small table reads its data via direct path when a parallel degree greater than 1 is used. The implication for buffer cache sizing is important: large analytical queries and parallel scans do not benefit from a larger buffer cache — they bypass it. The benefit of the buffer cache is concentrated in OLTP workloads that repeatedly access the same moderate-sized set of blocks. When sizing the buffer cache, the analytical workload can be ignored if it uses direct path or parallel query; only the OLTP working set — the set of index and table blocks accessed repeatedly by high-frequency single-row and range-scan queries — determines the required buffer cache size.

---

## Sizing the Buffer Cache

Oracle provides \`V$DB_CACHE_ADVICE\` specifically for empirical buffer cache sizing. This view contains rows for hypothetical cache sizes ranging from 10% to 200% of the current DEFAULT pool size, in 10% increments. For each hypothetical size, Oracle estimates the number of additional physical reads that would occur (\`ESTD_PHYSICAL_READS\`) and the ratio of those estimated reads to the current physical read count (\`ESTD_PHYSICAL_READ_FACTOR\`). A factor below 1.0 means fewer physical reads than currently observed — the current cache is too small. A factor above 1.0 means more physical reads than currently observed — the cache has been shrunk below current size for this row's hypothetical. The cache advice is populated continuously as the instance runs; after startup it requires at least a few hours of representative workload to produce meaningful estimates.

The "knee of the curve" is the key concept for interpreting \`V$DB_CACHE_ADVICE\`. Plot the estimated physical read factor against the hypothetical cache size; the curve drops steeply as size increases from small to moderate, then flattens as size grows further. The knee — the inflection point where the marginal benefit of additional cache drops sharply — identifies the optimal cache size. Beyond the knee, doubling the cache size reduces physical reads by only a few percentage points; before the knee, a 25% cache increase might reduce physical reads by 30%. Sizing the buffer cache at the knee of the curve gives the best performance-per-gigabyte tradeoff. In practice, add 20–25% headroom above the knee to accommodate growth in the hot working set over time.

With ASMM active (\`SGA_TARGET > 0\`), set \`DB_CACHE_SIZE\` to the knee-of-curve size as a minimum floor; Oracle will grow the buffer cache above this if SGA space is available. Monitor \`V$MEMORY_RESIZE_OPS\` to observe how Oracle has been redistributing SGA memory — if Oracle is repeatedly shrinking the buffer cache below the floor you set, the floor is too high for the available SGA; if Oracle consistently grows the buffer cache to its maximum, increase \`SGA_TARGET\` or reduce the share claimed by other SGA components. The working set concept is fundamental to right-sizing: the buffer cache only needs to hold the hot working set, not the entire database. A 500 GB database where OLTP transactions access only 25 GB of "hot" index and table blocks needs a 25–30 GB buffer cache, not a 500 GB one. \`V$BH\` and \`V$BUFFER_POOL_STATISTICS\` together provide the current utilisation picture: \`V$BH\` shows individual buffer headers and their touch counts, and \`V$BUFFER_POOL_STATISTICS\` gives aggregate statistics per pool including hit ratio, dirty buffer counts, and wait counts.

---

## Summary

The Oracle buffer cache is the foundation of database performance for OLTP workloads. Its modified LRU algorithm — based on touch counts and midpoint insertion rather than strict access recency — provides natural resistance to scan pollution while efficiently retaining genuinely hot blocks. The three buffer pools serve clearly differentiated purposes: the DEFAULT pool handles the general OLTP working set under ASMM management, the KEEP pool provides a permanent residence for critical small objects that must never be evicted, and the RECYCLE pool isolates large infrequent scans from the DEFAULT pool's working set. Assigning objects to the correct pool is one of the most impactful and underutilised buffer cache tuning actions available to DBAs.

The buffer cache hit ratio, while historically important, is an insufficient standalone tuning metric. A 99% hit ratio coexisting with 15 ms average \`db file sequential read\` latency is worse than a 95% hit ratio with 0.5 ms latency. The actionable metrics are wait event duration averages from \`V$SYSTEM_EVENT\`, physical reads per execution from \`V$SQL\`, and hot block identification from the \`P1\`/\`P2\`/\`P3\` columns of session wait events joined to \`V$BH\`. These three data sources together diagnose the vast majority of buffer cache performance problems more reliably than hit ratio ever could.

Hot block contention — expressed through \`buffer busy waits\`, \`read by other session\`, and \`latch: cache buffers chains\` wait events — requires object-level intervention: increasing sequence cache sizes, enabling ASSM to eliminate freelist contention, using reverse key or partitioned indexes to distribute index root block pressure, and assigning frequently accessed lookup tables to the KEEP pool. Direct path reads for large table scans protect the DEFAULT pool from scan pollution automatically in 11g+; understanding this mechanism ensures that large analytical workloads are correctly diagnosed as \`direct path read\` events that have no buffer cache impact, not as cache pressure problems requiring additional memory.

Sizing the buffer cache empirically via \`V$DB_CACHE_ADVICE\`'s knee-of-curve analysis, combined with workload monitoring through \`V$BUFFER_POOL_STATISTICS\` and \`V$MEMORY_RESIZE_OPS\`, gives the DBA the data needed to make defensible sizing decisions. The buffer cache is not a set-and-forget component: as data volumes grow, as query patterns evolve with new application features, and as batch workloads change, the hot working set changes. Routine monitoring for pool pressure, hot block contention patterns, and scan pollution — perhaps monthly for stable systems, more frequently during application changes — ensures that buffer cache configuration remains appropriate for the current workload rather than the workload that existed when the system was first sized.`,
};

async function main() {
  console.log('Inserting Oracle Buffer Cache concept post...');
  await db.insert(posts).values(post).onConflictDoUpdate({
    target: posts.slug,
    set: { ...post },
  });
  console.log('Inserted: "' + post.title + '"');
}

main().catch(console.error);
